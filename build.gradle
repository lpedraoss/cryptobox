plugins {
    id 'java'
}

group 'com.brosgor'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    // Agrega aquí las dependencias de tu proyecto
    // Ejemplo: implementation 'com.example:library:1.0'
}

test {
    // Configuración de pruebas
    // Ejemplo: testLogging {
    //              events 'passed', 'skipped', 'failed'
    //          }
}

// Otros bloques de configuración según tus necesidades